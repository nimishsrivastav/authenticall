# Prevent optional dependency issues
legacy-peer-deps=true

# Faster installs
prefer-offline=true
progress=true

# Engine strict checking
engine-strict=true

# Lockfile version
lockfile-version=2

# Auto install peer dependencies
auto-install-peers=true
